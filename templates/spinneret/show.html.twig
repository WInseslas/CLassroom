{% extends 'base.html.twig' %}

{% block title %}Spinneret{% endblock %}

{% block body %}
    <h1>Spinneret</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ spinneret.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ spinneret.name }}</td>
            </tr>
            <tr>
                <th>Course</th>
                <td>{{ spinneret.course }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_spinneret_index') }}">back to list</a>

    <a href="{{ path('app_spinneret_edit', {'id': spinneret.id}) }}">edit</a>

    {{ include('spinneret/_delete_form.html.twig') }}
{% endblock %}
